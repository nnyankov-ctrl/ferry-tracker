// =====================================================================
//  config.js — НАСТРОЙКИ САЙТА
//  Заполни значения ниже. index.html их использует автоматически.
// =====================================================================

window.FERRY_CONFIG = {

  // --- MQTT-брокер (HiveMQ Cloud), подключение из браузера ---
  // ВАЖНО: порт для WebSocket-TLS у HiveMQ Cloud = 8884, путь /mqtt.
  broker: {
    host: "d83ad83f9a7e43658e4e457eb8f8f53a.s1.eu.hivemq.cloud",
    port: 8884,
    path: "/mqtt",
    // Пользователь «только чтение» — тот, что заведён для сайта.
    // Да, он виден в коде страницы. Поэтому у него НЕТ прав на запись —
    // подделать позицию парома через него нельзя.
    username: "ferry-web",
    password: "0663458440",
    topic: "ferry/position",
  },

  // --- Координаты причалов (ОРИЕНТИРОВОЧНЫЕ — уточни по карте!) ---
  // Открой Google Maps, поставь точку ровно на слип парома, скопируй
  // широту/долготу и впиши сюда. От точности зависит расчёт направления и ETA.
   piers: {
    island:   { name: "Valentia (Knightstown)", lat: 51.925435, lon: -10.286475 },
    mainland: { name: "Reenard Point",           lat: 51.928492, lon: -10.277524 },
  },

  // Через сколько секунд без обновлений считать позицию устаревшей
  // (например, паром встал на ночь) и показывать её приглушённо.
  staleAfterSeconds: 120,
};
